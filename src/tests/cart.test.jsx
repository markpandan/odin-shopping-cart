import { describe, it } from "vitest";

describe("Cart page", () => {
  it("ProductSelectedItem component are being generated based on the cartItems array", () => {});

  it("Increments the specific product quantity of an array", () => {});

  it("Decrements the specific product quantity of an array", () => {});

  it("Allows to change the quantity using an input element");

  it("Calculates the total number products multiplied by quantity", () => {});
});
