import { describe, it } from "vitest";

describe("Product page", () => {
  it("Displays a specific product based on selected item or the ID specified", () => {});

  it("Adds the product to the cart", () => {});
});
